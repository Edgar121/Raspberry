#!/usr/bin/env python

import scrollphat
import math
import time
import sys
import random

buf = [0] * 11
bright = random.randint(1,100)

while True:
	try:
		for bright in range(1,10)
		scrollphat.update()
